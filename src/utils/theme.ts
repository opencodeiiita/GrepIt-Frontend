import { ThemeConfig } from "antd";

const theme: ThemeConfig  = {
    token: {
        
    },
    components: {
        Layout: {
            headerBg: ""
        },
        Button: {
            primaryColor: "#00000",
        }
    }
}

export default theme;